# VersionSeal 0.3.0 release verification

Date: 2026-09-23. Release tag: `v0.3.0`. Prepared from main at `b8cddee24c705c433a12261c8e9232a95f470996`; the tag identifies the release commit containing this receipt.

## Compatibility

Requires Kujo >=1.5.0. VERSION, package manifest, runtime profile, README badge, and changelog agree on 0.3.0. New records carry tool version 0.3.0; both runtime validation and JSON Schema retain 0.1.0/0.2.0 support. Schema/contract versions remain 1.0.0. Recovery requires upgrading all writers together; see [recovery.md](../recovery.md).

## Verification

All commands below passed locally on macOS:

- `kujo run tests/test.kujo`: 20 assertions, including emitted version, release-file/runtime agreement, schema acceptance, and legacy record validation.
- `bash scripts/validate.sh`: **200 assertions** across all suites and repeated interpreter coverage; 32 distinct writers, 32 same-ID contenders with one winner, and 16 concurrent recoveries.
- `../kujo/target/release/kujo run tests/test.kujo`: 20 assertions using the repository-prescribed executable.
- `./bin/versionseal --version --json`: version 0.3.0, minimum_kujo 1.5.0, supported_record_versions 0.1.0/0.2.0/0.3.0.
- `git diff --check`: passed.

The complete gate used installed Kujo 1.5.0; the pre-existing `eprint` type-check warning remains visible. Local verbose output is retained at `.tmp/release-0.3.0-validation.log`; concise checks are committed alongside this receipt.

GitHub-hosted cross-platform validation remains queued at release preparation. Prior successful CI runs establish historical coverage only, not a hosted pass for this tag. Release notes disclose the pending status. No tests were skipped or weakened to prepare this release.

Power-loss/network-filesystem durability and simultaneous raw record/history visibility remain outside the supported guarantees. Legacy locks without intents require offline review. These limits are unchanged by release preparation.
